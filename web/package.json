{
  "name": "apparatus-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "vite-node scripts/e2e.ts",
    "compile": "node ../scripts/compile-check.mjs",
    "sync": "../scripts/sync-contract.sh"
  },
  "dependencies": {
    "penpal": "^7.0.4",
    "three": "^0.185.1",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/three": "^0.185.4",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
